## Tasks

1. [] Create `PATCH /api/battles/` endpoint with a `:battleId` route param
2. [] Make a dedicated Auth controller

## Stuff I learned today

1. Using Namespace Imports for the backend is great for organization (like for my controllers and services). However, for my frontend it may be better
   to just use Named Imports so we can get the benefits of tree-shaking. Tree shaking doesn't matter in the backed though because the backend isn't bundled! haha.
2. I've learned a good system for keeping my private routes (routes I put after `router.use(authenticate)`) nice and consistent and orderly. How? By ordering via CRUD operations! Here's an example:

```typescript

router.post(some public route stuff);
// My authenticate Guardian Angel
router.use(authenticate);

// 1. CREATE
router.post(
  '/battle',
  validate(BattleValidator.createBattle),
  BattleController.createBattle,
);

// 2. READ (List first, then specific IDs. Also, make sure to put specific routes before dynamic routes!)
router.get('/battle', BattleController.listBattles);
router.get('/battle/somespecificbattle', BattleController.getSpecificBattle); // Here's a specific route that I gotta put before the dynamic route.
router.get('/battle/:id', BattleController.getBattle);

// 3. UPDATE
router.patch(
  '/battle/:id',
  validate(BattleValidator.updateBattle),
  BattleController.updateBattle,
);

// 4. DELETE
router.delete('/battle/:id', BattleController.deleteBattle);
```

## Scratchpad

1. Ohhhh we're getting clean now! I'm using Namespace Imports now for my Controllers, Services, and Validators! Sooooo clean lol.
